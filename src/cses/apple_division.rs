// There are n apples with known weights. Your task is to divide the apples into two groups so that the difference between the weights of the groups is minimal.
// Input
// The first input line has an integer n: the number of apples.
// The next line has n integers p_1,p_2,\dots,p_n: the weight of each apple.
// Output
// Print one integer: the minimum difference between the weights of the groups.

pub fn apple_division(s: String) -> Vec<String> {
    todo!()
}

// #[cfg(test)]
// mod tests {
//     use super::*;

//     #[test]
//     fn apple_division_test() {
//         assert_eq!(apple_division("aabac".to_string()), vec!["aaabc", "aaacb", "aabac", "aabca", "aacab", "aacba", "abaac", "abaca", "abcaa", "acaab", "acaba", "acbaa", "baaac", "baaca", "bacaa", "bcaaa", "caaab", "caaba", "cabaa", "cbaaa"]);
//     }
// }
